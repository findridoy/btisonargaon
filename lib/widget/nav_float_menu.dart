import 'package:flutter/material.dart';

class NavFloatMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: RaisedButton(
        onPressed: null,
        child: Text("Tap me"),
      ),
    );
  }
}
